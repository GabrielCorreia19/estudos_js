// Factory é uma função que retorna um objeto
// A tradução de Factory é = fábrica --> Fabricar uma instância de um objeto para a função
function pessoa() {
  return {
    name: "aaa",
    sobrenome: "bbb",
  };
}

console.log(pessoa());
